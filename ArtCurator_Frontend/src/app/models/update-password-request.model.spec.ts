import { UpdatePasswordRequest } from './update-password-request.model';

describe('UpdatePasswordRequest', () => {
  it('should create an instance', () => {
    expect(new UpdatePasswordRequest()).toBeTruthy();
  });
});
